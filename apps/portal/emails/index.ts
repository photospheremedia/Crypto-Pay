/**
 * Email Templates Index
 * All React Email templates exported from this central location
 */

// Admin & Invitation Emails
export { AdminInviteEmail, type AdminInviteEmailProps } from './admin-invite';

// Re-export render utility for server-side rendering
export { render } from '@react-email/render';
