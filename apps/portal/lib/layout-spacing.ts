/** Gap between sticky header (h-16) and page content — use on layout <main> only. */
export const mainBelowHeaderClass = "pt-6 sm:pt-8";

/** Sticky sidebar offset: header height + main top padding. */
export const stickyBelowHeaderClass = "top-20 sm:top-[5.5rem]";
