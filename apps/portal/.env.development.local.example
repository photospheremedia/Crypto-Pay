# Loaded automatically in `pnpm dev:portal` (overrides NEXT_PUBLIC_APP_URL from .env.local)
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Optional: used by `pnpm dev:setup` (defaults below if unset)
# LOCAL_DEV_EMAIL=merchant@example.com
# LOCAL_DEV_PASSWORD=CryptoPayDev!2026
