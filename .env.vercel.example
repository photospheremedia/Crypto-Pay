# Vercel CLI + GitHub Actions (never commit real values)
# Copy: cp .env.vercel.example .env.vercel
#
# Token: https://vercel.com/account/tokens
VERCEL_TOKEN=

# Optional — auto-filled by `pnpm vercel:link` into .vercel/project.json
# VERCEL_ORG_ID=
# VERCEL_PROJECT_ID=
