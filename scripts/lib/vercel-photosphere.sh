# PhotoSphere Media — canonical Vercel scope for Crypto Pay (source in .vercel/project.json).
# Do not use personal / other Vercel accounts for this repo.

VERCEL_PROJECT_NAME="${VERCEL_PROJECT_NAME:-crypto-pay-portal}"
# PHOTOSPHEREMEDIA's projects (slug). Override with VERCEL_TEAM_ID if you use --scope by id.
VERCEL_TEAM_SLUG="${VERCEL_TEAM_SLUG:-photospheremedia-s-projects}"
VERCEL_TEAM_ID="${VERCEL_TEAM_ID:-team_AflYR0tFtYDkhqMjaEg0vyQj}"
VERCEL_FORBIDDEN_ACCOUNT_PATTERN="${VERCEL_FORBIDDEN_ACCOUNT_PATTERN:-skullcandy}"
